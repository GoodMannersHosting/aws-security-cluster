# Production (keeper.goodmanners.services): bind-mount data under /opt/stacks/.
# Stack secrets live in git as stacks/*/secrets.enc.env (SOPS + age).
# Doco-CD decrypts env_files at deploy time; age key stays on the host only.
# MAX_CONCURRENT_DEPLOYMENTS=1 serializes deploys in list order.

---
name: hcloud-traefik
working_dir: stacks/traefik
webhook_filter: ^refs/heads/main$
compose_files:
  - docker-compose.yaml
env_files:
  - secrets.enc.env

---
name: hcloud-authentik
working_dir: stacks/authentik
webhook_filter: ^refs/heads/main$
compose_files:
  - compose.yaml
env_files:
  - secrets.enc.env

---
name: hcloud-openbao
working_dir: stacks/openbao
webhook_filter: ^refs/heads/main$
compose_files:
  - compose.yaml
env_files:
  - secrets.enc.env

---
name: hcloud-alloy
working_dir: stacks/alloy
webhook_filter: ^refs/heads/main$
compose_files:
  - compose.yaml
env_files:
  - secrets.enc.env

---
name: hcloud-doco-cd
working_dir: stacks/doco-cd
webhook_filter: ^refs/heads/main$
force_recreate: false
timeout: 300
compose_files:
  - compose.yaml
  - compose.sops.yaml
env_files:
  - secrets.enc.env
