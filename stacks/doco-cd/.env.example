# Hostname for Traefik (HTTPS). Webhook URL: https://<host>/v1/webhook
DOCO_CD_HOSTNAME=doco-cd.goodmanners.services

# HMAC secret (GitHub/GitLab/Gitea webhook "Secret"); generate with: openssl rand -base64 40
WEBHOOK_SECRET=

# Private repo clone token (GitHub fine-grained or classic PAT). Optional for public repos.
GIT_ACCESS_TOKEN=

# Serialize deploys so stacks apply in .doco-cd.yml order (avoids openbao before postgres).
MAX_CONCURRENT_DEPLOYMENTS=1

# Optional
DOCO_CD_TAG=0.90.1
TZ=UTC
LOG_LEVEL=INFO

# Path on the VPS (install-prod.sh uses /opt/stacks/doco-cd/.env)
DOCO_CD_ENV_FILE=/opt/stacks/doco-cd/.env

# SOPS + age (encrypted stack files in git):
#   cp ../../.sops.yaml.example ../../.sops.yaml   # then set age: to your public key
#   age-keygen -o sops_age_key.txt && chmod 600 sops_age_key.txt
# Either:
#   docker compose -f compose.yaml -f compose.sops.yaml up -d
# Or set in this directory:
#   COMPOSE_FILE=compose.yaml:compose.sops.yaml
